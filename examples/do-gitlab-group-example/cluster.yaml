cluster:
  env: sandbox
  enabled: yes
  domain:
    name: do-gitlab-group-example.lazyorange.xyz
    letsEncryptEmail: vymarkov@gmail.com
  cloud:
    provider: digitalocean
  sso:
    oauth2_proxy:
      enabled: yes
  stacks:
    ingress:
      enabled: yes
      kong:
        enabled: yes
      cert_manager:
        enabled: yes
      external_dns:
        enabled: yes
    logging:
      enabled: no
      elastic_stack:
        enabled: yes
      apm:
        enabled: yes
