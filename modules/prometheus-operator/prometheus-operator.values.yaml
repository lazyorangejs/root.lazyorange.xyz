# https://github.com/helm/charts/issues/19928#issuecomment-580403978
prometheusOperator:
  admissionWebhooks:
    enabled: false
    patch:
      enabled: false

  tlsProxy:
    enabled: false

  resources:
    limits:
      cpu: 200m
      memory: 200Mi
    requests:
      cpu: 100m
      memory: 100Mi

prometheus:
 resources:
    requests:
      memory: 400Mi